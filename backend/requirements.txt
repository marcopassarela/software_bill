# ------------------------------------------------------------------
# DEPENDÊNCIAS DE DESENVOLVIMENTO (máquina local / migrations / testes)
# ------------------------------------------------------------------
# Produção (Vercel) usa o requirements.txt da raiz, que é enxuto de
# propósito para não estourar a cota de Functions Storage.
-r ../requirements.txt

uvicorn[standard]==0.34.0
alembic==1.14.0
pytest==8.3.4
httpx==0.28.1

# Só necessário se você ligar REALTIME_SSE_ENABLED=1 em um servidor
# persistente (Render, Oracle Cloud, VPS). Nunca em serverless.
redis==5.2.1